{
  "name": "forward-proxy",
  "version": "1.0.0",
  "description": "A path-based HTTP/HTTPS forward proxy with user authentication and route-based access control. This proxy routes requests to different upstream services based on URL paths and enforces user-based permissions.",
  "main": "proxy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
